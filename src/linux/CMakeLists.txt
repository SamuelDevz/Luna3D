if(BUILD_X11)
    add_subdirectory(Xlib)
elseif(BUILD_XCB)
    add_subdirectory(XCB)
elseif(BUILD_WAYLAND)
    add_subdirectory(Wayland)
else()
    message(FATAL_ERROR "Please choose a window API (BUILD_X11, BUILD_XCB or BUILD_WAYLAND).")
endif()